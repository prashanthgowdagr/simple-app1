apiVersion: v1
kind: ReplicationController
metadata:
  name: myapp-rc
  labels:
    name: myapp-rc
spec:
  template:
    metadata:
     name: myapp
     labels:
      name: myapp
    spec:
      containers:
      - name: myapp
        image: httpd:latest
  replicas: 2
        